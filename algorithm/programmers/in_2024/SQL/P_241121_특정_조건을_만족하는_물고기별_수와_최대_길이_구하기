-- 코드를 작성해주세요

SELECT A.FISH_COUNT AS FISH_COUNT, A.LEN_MAX AS MAX_LENGTH, A.FISH_TYPE AS FISH_TYPE
FROM
(
    SELECT AVG(IFNULL(LENGTH,10)) AS LEN_AVG, FISH_TYPE, COUNT(*) AS FISH_COUNT, MAX(LENGTH) LEN_MAX
    FROM FISH_INFO
    GROUP BY FISH_TYPE
) A
WHERE A.LEN_AVG >= 33
ORDER BY FISH_TYPE